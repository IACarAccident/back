# back

Pour faire fonctionner le projet, déplacer tous les repositories de l'organisation dans un dossier racine.

Ensuite deplacer le docker-compose.yml dans ce dossier racine.
Lancer votre service Docker puis lancer la commande docker compose up --build

Le projet devrait se lancer dans docker.
